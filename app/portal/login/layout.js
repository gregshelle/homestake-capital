export const metadata = {
  title: 'HomeStake Capital | Investor Login',
};

// Auth pages use a standalone layout — no portal shell/sidebar
export default function PortalAuthLayout({ children }) {
  return children;
}
