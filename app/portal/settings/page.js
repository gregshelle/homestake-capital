import { AccountSettings } from '../../../components/portal-ui';

export const metadata = {
  title: 'HomeStake Capital | Account Settings',
};

export default function SettingsPage() {
  return <AccountSettings />;
}
